# CLAUDE.md

Working conventions for this repo live in a single place. See @AGENTS.md.

In particular: run `npm run test:e2e` (and `npm run build`) before opening a PR,
and add regression coverage for any change to game behaviour.
